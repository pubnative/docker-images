This image extends jupyter all spark notebook images with
- requirements
- spark default configuration

It pushes to target `annoyingllama/jupyter`

Instructions:
```shell
# build
make build

# push
make push
```

